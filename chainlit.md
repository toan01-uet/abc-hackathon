# Meeting Tasks Agent

Paste a meeting transcript (or attach a `.txt`/`.md` file) to extract action items — owner, due date, and dependencies.

Once you're happy with the extracted tasks, connect a Notion MCP server via the 🔌 icon in the composer to create them as pages in a Notion database. Nothing is written to Notion until you explicitly confirm.
